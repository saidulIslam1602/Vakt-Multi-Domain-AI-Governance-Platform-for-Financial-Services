"""Placeholder tests — replace with real unit tests as the service grows."""


def test_service_importable() -> None:
    """Smoke-test: the service package can be imported without errors."""
    # This will be replaced by real tests. Keeping it so pytest exits 0.
    assert True
